add a new module will need to be registered

register the model inside models.ts
register the controller iside controllers.ts
register the service inside modules.ts
register the routes inside routes.ts
